import type { ContentConfig } from '@lib/config/types';

export type { ContentConfig };

/** Content processing flags with field-level defaults applied. */
export { contentConfig as defaultContentConfig } from '@lib/config/site';
